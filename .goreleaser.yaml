version: 2

project_name: ch

changelog:
  disable: true

builds:
  - id: ch
    main: ./cmd/ch
    binary: ch
    ldflags: >-
      -s -w
      -X github.com/openclaw/ch/internal/cmd.version={{ .Tag }}
      -X github.com/openclaw/ch/internal/cmd.commit={{ .ShortCommit }}
      -X github.com/openclaw/ch/internal/cmd.date={{ .Date }}
    env:
      - CGO_ENABLED=0
    targets:
      - linux_amd64
      - linux_arm64
      - darwin_amd64
      - darwin_arm64
      - windows_amd64
      - windows_arm64

archives:
  - builds:
      - ch
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt
